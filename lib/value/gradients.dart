import 'package:flutter/material.dart';
import 'colors.dart';

class AppGradients {
  /// Default
  static LinearGradient transparentAndBlack = LinearGradient(
    begin: Alignment.topCenter,
    end: Alignment.bottomCenter,
    colors: [AppColors.transparent,AppColors.primaryDark.withOpacity(0.5)],
  );
  /// Primary

  /// Other
}
